# BI Ecom (Digital Impact) — entorno consolidado para todo el stack.
# Crear venv:  py -3.11 -m venv venv
#              venv\Scripts\python -m pip install -r requirements.txt
# Los batches usan venv\Scripts\python.exe (ver instalar_tareas_bi.ps1).
#
# IMPORTANTE: versiones PINNEADAS al entorno global 3.11 que llevaba años
# corriendo bien. NO usar rangos abiertos (>=): pandas 3.x / numpy 2.x
# rompen los scripts (p.ej. fillna('') en columnas float64). Replicar el
# entorno probado es la regla — ver memoria project_bi_ecom_diagnostico.

# Datos / SQL
pandas==2.0.3
numpy==1.25.2
SQLAlchemy==2.0.41
pyodbc==5.2.0
openpyxl==3.1.2
python-dotenv==1.0.1

# HTTP (PageSpeed, APIs)
httpx==0.28.1
requests==2.31.0
tqdm==4.66.2

# Google (GA4 + Sheets)
google-analytics-data==0.18.0
google-api-python-client==2.149.0
google-auth==2.49.2
gspread==6.2.1
gspread-formatting==1.2.1
oauth2client==4.1.3
pytz==2023.3

# GUI (tools auxiliares)
customtkinter
tkcalendar
flask==3.0.3
